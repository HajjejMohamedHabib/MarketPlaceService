{% extends 'template.html.twig' %}

{% block title %}Update{% endblock %}
{% block header %}
    <header id="head" class="secondary"></header>
{% endblock %}

{% block body %}
    <!-- container -->
    <div class="container">

        <ol class="breadcrumb">
            <li><a href="index.html">Home</a></li>
            <li class="active">Profile Update</li>
        </ol>

        <div class="row">

            <!-- Article main content -->
            <article class="col-xs-12 maincontent">
                <header class="page-header">
                    <h1 class="page-title">Profile Update</h1>
                </header>

                <div class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <h3 class="thin text-center">Update your account</h3>
                            <p class="text-center text-muted">Update your account to manage personal details, enhance security settings, and customize preferences for a more personalized experience. </p>
                            <hr>

                            {% for flash_error in app.flashes('verify_email_error') %}
                                <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
                            {% endfor %}

                            <h1>Register</h1>
                            {{ form_start(registrationForm,{'attr':{'novalidate':'novalidate'}}) }}
                            <div class="top-margin">
                                {{ form_label(registrationForm.name) }}
                                {{ form_widget(registrationForm.name,{'attr':{'class':'form-control'}}) }}
                                {{ form_errors(registrationForm.name) }}
                            </div>
                            <div class="top-margin">
                                {{form_label(registrationForm.phone_number) }}
                                {{ form_widget(registrationForm.phone_number,{'attr':{'class':'form-control'}}) }}
                                {{ form_errors(registrationForm.phone_number) }}
                            </div>
                            <div class="top-margin">
                                {{form_label(registrationForm.address) }}
                                {{ form_widget(registrationForm.address,{'attr':{'class':'form-control'}}) }}
                                {{ form_errors(registrationForm.address) }}
                            </div>
                            <div class="top-margin">
                                {{form_label(registrationForm.email) }}
                                {{ form_widget(registrationForm.email,{'attr':{'class':'form-control'}}) }}
                                {{ form_errors(registrationForm.email) }}
                            </div>
                            <div class="top-margin">
                                {{form_label(registrationForm.plainPassword,'password') }}
                                {{ form_widget(registrationForm.plainPassword,{'attr':{'class':'form-control'}}) }}
                                {{ form_errors(registrationForm.plainPassword) }}
                            </div>
                            <div class="top-margin">
                                {{ form_widget(registrationForm.photo) }}
                            </div>
                            <hr>

                            <div class="row">
                                <div class="col-lg-8">
                                    {{ form_widget(registrationForm.agreeTerms) }}
                                    {{ form_label(registrationForm.agreeTerms,"I've read the") }}
                                    <a href="page_terms.html">Terms and Conditions</a>
                                    {{ form_errors(registrationForm.agreeTerms) }}
                                </div>
                                <div class="col-lg-4 text-right">
                                    <button class="btn btn-action" type="submit">Update</button>
                                </div>
                            </div>



                            {{ form_end(registrationForm) }}
                        </div>
                    </div>

                </div>

            </article>
            <!-- /Article -->

        </div>
    </div>	<!-- /container -->
{% endblock %}

